"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
import { AlertCircle, ArrowRight, CheckCircle2, ExternalLink, LoaderCircle } from "lucide-react";
import SiteHeader from "@/components/site-header";

export default function Register(){const [loading,setLoading]=useState(false),[error,setError]=useState(""),[success,setSuccess]=useState(""),[devUrl,setDevUrl]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const f=new FormData(e.currentTarget);if(f.get("password")!==f.get("confirm")){setError("Passwords do not match.");setLoading(false);return}try{const response=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:f.get("name"),nickname:f.get("nickname"),email:f.get("email"),house:f.get("house"),password:f.get("password")})});const data=await response.json();if(!response.ok)throw new Error(data.error);setSuccess(data.message);setDevUrl(data.developmentUrl||"")}catch(e){setError(e instanceof Error?e.message:"Registration failed.")}finally{setLoading(false)}}return <><SiteHeader/><main className="auth-page"><section className="auth-visual"><div><span className="eyebrow">The story continues</span><h1>Take your place in our living yearbook.</h1><p>Every profile adds another chapter to the story we started together in Makurdi. Membership requests are checked against the official Class of 2012 list.</p></div></section><section className="auth-form-wrap"><form className="auth-box" onSubmit={submit}><span className="eyebrow">Join the community</span><h2>Request membership</h2><p>Tell us how we knew you at the Mount.</p>{error&&<div className="form-alert error"><AlertCircle size={16}/>{error}</div>}{success&&<div className="form-alert success"><CheckCircle2 size={17}/><span>{success}{devUrl&&<a href={devUrl}>Open development verification link <ExternalLink size={13}/></a>}</span></div>}<div className="form-group"><label htmlFor="name">Full name</label><input className="input" id="name" name="name" required minLength={3} autoComplete="name" placeholder="Your full name"/></div><div className="form-group"><label htmlFor="nickname">School nickname <span style={{fontWeight:400}}>(optional)</span></label><input className="input" id="nickname" name="nickname" placeholder="What did the boys call you?"/></div><div className="form-group"><label htmlFor="email">Email address</label><input className="input" id="email" name="email" type="email" required autoComplete="email" placeholder="you@example.com"/></div><div className="form-group"><label htmlFor="house">House</label><select className="select" id="house" name="house" required defaultValue=""><option value="" disabled>Select your house</option><option>Blue House</option><option>Red House</option><option>Green House</option><option>Yellow House</option></select></div><div className="form-grid"><div className="form-group"><label htmlFor="password">Password</label><input className="input" id="password" name="password" type="password" required minLength={10} autoComplete="new-password"/></div><div className="form-group"><label htmlFor="confirm">Confirm</label><input className="input" id="confirm" name="confirm" type="password" required minLength={10} autoComplete="new-password"/></div></div><p className="password-hint">Use at least 10 characters with uppercase, lowercase and a number.</p><button className="btn btn-navy" disabled={loading||Boolean(success)}>{loading?<LoaderCircle className="spin" size={16}/>:null}{loading?"Creating request…":"Submit membership request"}<ArrowRight size={16}/></button><p className="auth-switch">Already verified? <Link href="/login">Sign in</Link></p></form></section></main></>}
