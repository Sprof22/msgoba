"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
import { ArrowLeft, CheckCircle2, ExternalLink } from "lucide-react";
import SiteHeader from "@/components/site-header";
export default function Forgot(){const [message,setMessage]=useState(""),[dev,setDev]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:f.get("email")})});const d=await r.json();setMessage(d.message||d.error);setDev(d.developmentUrl||"")}return <><SiteHeader/><main className="status-page"><form className="status-card card" onSubmit={submit}><span className="eyebrow">Account recovery</span><h1>Reset your password</h1><p>Enter the email address connected to your membership account.</p>{message&&<div className="form-alert success"><CheckCircle2 size={16}/><span>{message}{dev&&<a href={dev}>Open development reset link <ExternalLink size={12}/></a>}</span></div>}<div className="form-group"><label htmlFor="email">Email address</label><input className="input" id="email" name="email" type="email" required/></div><button className="btn btn-navy">Send reset link</button><Link className="text-link status-back" href="/login"><ArrowLeft size={14}/>Back to sign in</Link></form></main></>}
